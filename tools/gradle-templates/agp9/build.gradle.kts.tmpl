// Top-level build file for AeroLang projects (AGP 9.x)
// AGP 9.0+ requires Kotlin Gradle Plugin as a runtime dependency.
// Generated by aero-gradle — run 'aero-gradle upgrade' to update.

plugins {
    id("com.android.application") version "{{AGP_VERSION}}" apply false
    // AGP 9.0 has a runtime dependency on KGP — must be declared here
    id("org.jetbrains.kotlin.android") version "{{KOTLIN_VERSION}}" apply false
}
